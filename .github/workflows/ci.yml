#name: Hello CI

#on:
  # push:
  # pull_request:
  # workflow_dispatch:

#jobs:
 # hello:
  #  runs-on: ubuntu-latest
   # steps:
   #   - name: Print hello
   #     run: echo "Hello from GitHub Actions!"
